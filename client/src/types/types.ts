export type loginPageTypes = {
    heading: string,
    description: string,
    list: Array<{item:string,img:string }>
}
export type registrationPageTypes = {
    heading: string,
    description: string,
    headingCard: string,
    headingConfirm: string,
    sms: string
}